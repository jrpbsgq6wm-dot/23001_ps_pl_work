# 数据帧格式

状态：待 PS/PL 工程师共同确认

需要明确：

- 魔数；
- 协议版本；
- 帧类型；
- 头部长度；
- 总长度；
- 序号；
- 时间戳；
- 通道数；
- 点数；
- 采样率；
- 数据类型；
- 端序；
- 对齐方式；
- 负载长度；
- 校验方式；
- 错误标志。

## 帧结构

| 字段 | 字节偏移 | 字节数 | 类型 | 说明 |
|---|---:|---:|---|---|
| MAGIC | 待定义 | 待定义 | 待定义 |  |
| VERSION | 待定义 | 待定义 | 待定义 |  |
| TYPE | 待定义 | 待定义 | 待定义 |  |
| HEADER_LENGTH | 待定义 | 待定义 | 待定义 |  |
| FRAME_LENGTH | 待定义 | 待定义 | 待定义 |  |
| SEQUENCE | 待定义 | 待定义 | 待定义 |  |
| PAYLOAD | 待定义 | 待定义 | 待定义 |  |
| CRC | 待定义 | 待定义 | 待定义 |  |

PS 和 PL 在此表确认前，不得各自定义一套不兼容的帧头。

