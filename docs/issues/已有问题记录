# 20260916 代码问题跟踪记录

检查日期：2026-09-18  
检查范围：`src/`、`include/`、当前 `system.dts` 以及 S231 新 DMA/CBF/NREAD 流程。  
本文件记录问题、整改状态和已完成的源码调整；`[ ]`表示仍有待确认或待补充的内容。

参考代码说明：`Linux_source_new (2)`是PL侧为验证寄存器、DMA、CBF和NREAD接口而制作的简化单帧样例，数据来源、参数加载方式和触发方式不等同于本工程的湿端TCP连续接收业务。后续移植时只参考其硬件接口时序和数据处理逻辑，不能直接照搬它的输入数据来源、单帧控制流程或参数文件流程。

状态说明：

- `[ ]` 待整改或待确认
- `[x]` 已处理或已确认
- `P0` 会导致崩溃、越界、数据错帧或新流程无法稳定运行
- `P1` 会导致业务结果错误、丢帧或现场偶发异常
- `P2` 维护性、兼容性或后续优化问题

## 一、UIO和寄存器映射

### 1.1 湿端寄存器指针仍为NULL

- 状态：`[x]`
- 优先级：`P0`
- 说明：`fpga_init.c` 已增加 `uio_wet_fpga_register` 和 `uio_make_fpga_register` 的初始化，并完成：
  - `ptr_wet_fpga_register_data = uio_wet_fpga_register.mem_ptr`
  - `ptr_mark_registers = uio_make_fpga_register.mem_ptr`
- 现在通过物理地址匹配设备树资源，不再假定它们固定对应 `/dev/uio11` 和 `/dev/uio13`。
- 启动日志会打印实际匹配到的设备路径、物理地址和映射大小，现场仍需确认这两项映射成功。

### 1.2 `uio_make_fpga_register`使用了错误的物理地址宏

- 状态：`[x]`
- 优先级：`P1`
- 文件：`include/fpga_init.h`、`src/fpga_init.c`
- 已新增规范宏`DDR_MAKE_FPGA_REGISTER_ADDR`，并将`uio_make_fpga_register`改为匹配`0x43c30000`。
- 原拼写错误的`DDR_MAKE_FPGA_REFIGSTER`仅作为兼容别名保留，不再用于初始化。

### 1.3 UIO初始化失败后仍继续运行

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/fpga_init.c`、`src/main.c`
- `uio_init()`现在返回成功/失败，并检查UIO设备打开、sysfs地址/大小读取、映射大小和`mmap()`结果。
- `fpga_interface_init()`任意一个映射失败都会返回错误，`main()`检测失败后立即退出，不再给无效指针赋值或启动线程。
- 原先未校验的`get_memory_size()`已由带返回值检查的sysfs读取函数替代。

### 1.4 UIO编号全部硬编码

- 状态：`[x]`
- 优先级：`P1`
- 文件：`include/fpga_init.h`、`src/fpga_init.c`
- 已取消初始化阶段对固定`/dev/uio0`到`/dev/uio14`和固定sysfs路径的依赖。
- 程序启动时扫描`/sys/class/uio/uio*/maps/map0/addr`，按目标物理地址匹配，再读取`map0/size`并校验期望大小，最后生成并打开实际的`/dev/uioX`。
- 当前采用物理地址和映射大小作为唯一匹配条件；对于`generic-uio`，UIO的`name`可能由驱动实现决定，不作为稳定匹配依据。

## 二、湿端TCP接收和数据边界

### 2.1 湿端四字节数据头接收方式

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/wettodry.c:353`、`src/wettodry.c:263`
- 已确认：当前协议就是由湿端发送四字节数据头，现有接收流程按该业务协议使用，不在本轮整改。
- 本项标记为“已确认无需修改”，不代表TCP本身具有消息边界；如果以后接入不能保证完整四字节发送的客户端，需要重新评估。

### 2.2 湿端数据长度校验

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/wettodry.c:261-279`
- 已确认：当前湿端数据来源和长度由既定业务协议保证，本项目暂不增加额外长度校验逻辑。
- 本项标记为“已确认无需修改”，后续若允许非受控客户端直接发送数据，再单独增加边界校验。

### 2.3 接收失败后的旧缓冲区处理

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/wettodry.c:263-279`、`src/wettodry.c:353-400`
- 已确认：按照当前连接管理和湿端业务流程，本项不作为当前版本整改内容。
- 后续若出现断线重连后旧缓冲区被重复处理，再单独增加缓冲区有效标志和接收状态清理。

### 2.4 `recv_socket()`返回长度处理

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/tcptrans_link.c:125-143`及多个调用点。
- 已确认：当前湿端发送端和协议流程满足现有`recv_socket()`调用约定，本项暂不修改。
- 本项标记为“已确认无需修改”，若以后协议允许短包或不可信发送端，需要重新评估。

## 三、新湿端到PL流程

### 3.1 上一帧IQ与当前帧Frame参数组合

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/beam_down.c:2028-2093`
- 当前PL输入使用`IQData_Last`中的上一ping IQ：`IQData_Last + 128`。
- 但当前Frame参数、传感器数据、声速和横摇数据主要由当前`IQData`解析后更新。
- 已确认：当前设计就是“上一帧IQ + 当前帧Frame/传感器参数”，这是流水处理带来的既定N-1数据关系，不属于当前版本错误。
- 后续调试时需要保留该时序约定，不能仅因为IQ地址使用`IQData_Last`就改成同一帧绑定，否则会改变现有PL输入时序。

### 3.2 新流程仍保留旧的384字节采样点计算

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/beam_down.c:653-671`
- `Set_Fpga_To_Dsp_Para()`仍用`IQ_Data_Length / 384 / SAMPLE_FACTOR`计算`Ad_Sn_After`和`Mems_Num`。
- 新S231流程使用`IQ_Data_Length / 832`得到IQ点数。
- 这会让旧的`Ad_Sn_After`、`Mems_Num`和新CBF的`IQ_POINTS`不一致，并把错误值写入Frame参数区或旧全局状态。
- 已确认：当前版本暂不修改，保留新旧两套计算逻辑，后续业务联调时再统一处理。

### 3.3 声速更新晚于系数计算

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c:2058-2071`、`src/beam_down.c:377-397`
- 当前先执行`Set_BeamDown_Fpga_Para()`，其中`Set_Coe_Value()`使用旧的`Manual_SoundSpeed`；随后才执行`Set_SoundSpeed_Value()`。
- 因此`Roll_Coe`和`Tao_Coe`可能基于上一帧声速计算。
- 已确认：当前版本暂不修改该执行顺序，后续根据PL实际使用的系数接口再处理。

### 3.4 `Mems_Num`和实际Roll有效点数来源不一致

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c:638-660`、`src/beam_down.c:932-996`
- `Recv_SensorSumNum`表示实际累计横摇点数，但`Mems_Num`按旧的声学采样点数除以200计算。
- 新Frame同时携带横摇数组和有效数量，必须确认DSP实际读取的是哪个字段，并保证数量与数组有效区一致。
- 已确认：当前版本暂不修改`Mems_Num`来源和相关Frame字段，保留现有业务逻辑。

### 3.5 Roll时间单位可能错误

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c:610-638`、`src/beam_down.c:1026-1027`
- `Motion_Time`和`Roll_Time_Diff`当前以秒计算和保存，协议文档中的`Roll_Time_Diff`需要按毫秒确认。
- 代码只在注释打印时乘了1000，实际写入Frame的数据没有转换。
- `Roll_Sequential_Value`虽然做了`/100`，但仍需确认最终单位是否为度。
- 已确认：当前版本暂不修改Roll时间和角度单位，保留现有数据处理方式。

### 3.6 Roll数组尾部可能残留上一帧数据

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c:865`、`src/beam_down.c:1026-1027`
- `fill_frame()`每次把完整400个Roll点复制到Frame，但有效点数量可能少于400。
- 如果全局Roll数组尾部没有在每帧按有效数量清零，DSP在读取数量判断错误时会读到上一帧残留值。
- 应明确由`mems_num`控制读取，或每帧清理有效区之外的数组。
- 已确认：当前版本暂不修改Roll数组清理和有效数量处理方式。

### 3.7 CFG、W3、W4寄存器的参考代码使用情况

- 状态：`[ ]`（已完成第一阶段，完整表参数和W3/W4仍待PL协议确认）
- 优先级：`P1`
- 文件：`src/fpga_init.c:115-123`、`src/beam_down.c`
- 参考代码确实使用了`CFG`寄存器：
  - `Linux_source_new (2)/src/s231_platform.c`映射`0x43C10400`为`cfg_regs`。
  - `Linux_source_new (2)/src/s231_tcp.c`通过参数下发写入Roll表、Sinc系数、Full Apod和Sub Apod。
  - 角度表写入CBF的`0x100/0x104/0x108`寄存器，不是写入W3或W4。
  - `s231_cbf_pre_srio`工具也有一套直接加载CFG参数表的流程。
- 参考代码中没有找到`W3`或`W4`寄存器的映射、写入或配置逻辑。
- `frequency_code`只在独立的`s231_cbf_smoke`测试程序中写过，未发现它属于主业务长期配置流程。
- 当前工程虽然完成了`ptr_ddr_cfg`、`ptr_ddr_w3`、`ptr_ddr_w4`映射，但没有发现业务写入；因此`CFG`是否已由其他流程下发仍需确认，W3/W4暂不作为参考代码要求整改。
- 注意：参考代码中的CFG写入主要服务于单帧验证，参数可能来自测试工具、参数文件或模拟输入，并不代表本工程必须照搬该数据来源。当前工程需要进一步确认如何把显控/湿端实际配置转换为CFG表写入。
- 整改前提已修正：按照当前PL接口约定，所有“需要PS写入的可写寄存器”都应在每帧启动前重新写入一次，不能仅在启动或显控配置变化时写入。
- 建议整改方向：
  - 在`Set_Dsp_TransBuf()`中形成统一的“本帧PL参数写入阶段”，在写`CBF CONTROL.START`之前完成CFG、CBF、W2、W3/W4（若协议要求）以及DMA描述符的本帧配置。
  - CFG寄存器按参考代码顺序逐项写入Roll表、Sinc、Full Apod和Sub Apod；角度表写入CBF的`0x100/0x104/0x108`。每一帧都重新写入，写完后再启动DMA和CBF。
  - 现有显控参数结构中没有完整的Sinc/Apod/256点角度表，不能直接用`Beam_Num`或旧角度参数伪造这些表；必须明确这些表在湿端数据、显控配置、配置文件或固定程序数据中的来源。
  - Frame中的`Roll_Time_Diff`和`Roll_Sequential_Value`是逐帧动态数据，只有在PL协议明确CFG Roll表就是这两类数据时，才能把它们转换后写入`roll_wdata`；不能仅凭名称直接复用。
  - 参考代码没有使用W3/W4，因此它们的每帧写入顺序和数据来源必须以当前PL文档为准，不能从参考程序推断。若PL确认需要，应把它们纳入同一个本帧PL参数写入阶段。
- `MAGIC`、`VERSION`、`STATUS`、`NREAD_EVENT_COUNT`等只读/状态寄存器不能按“所有寄存器”写入；每帧只写协议标明为PS写入的寄存器，并读取状态寄存器做校验。
- CFG、CBF、W2、W3、W4结构体指针应使用`volatile`，并在关键寄存器写入后使用写屏障，避免编译器重排或合并MMIO操作。
- 已完成第一阶段修改：`src/beam_down.c`新增统一的`S231_ProgramPlFrameParameters()`逐帧配置入口，并在`Set_Dsp_TransBuf()`中放置于`CBF CONTROL.START`之前。
- 已按参考代码逐帧写入CFG动态横摇参数：`roll_update_q29`、`roll_update_source`、`roll_update_push`，并将`roll_update_index`更名为与PL定义一致的`roll_update_source`。
- 已增加浮点弧度到有符号Q3.29的饱和转换和有效性检查；非法横摇值不会继续写入PL。
- 已兼容内置/外置惯导两条现有数据路径：内置惯导从`Roll_Sequential_Value[]`的最新角度值转换为弧度，外置惯导继续使用已经计算好的`Roll_Value`。
- 当前没有把`PWMFreq`直接写入`frequency_code`，因为前者是实际频率值，后者是PL编码，二者映射关系尚未确认；也没有用旧参数伪造Sinc/Apod/角度表。
- 已为W3/W4增加逐帧配置预留函数，目前只检查映射，不写未经PL确认的字段。待PL提供字段所有权和写入时序后，在该入口中补充具体寄存器操作。
- 后续必须向PL/DSP确认以下三项，确认前3.7不能标记为整体完成：
  1. **参数表数据来源**：明确Sinc、Full Apod、Sub Apod以及256点角度表由湿端数据、显控配置、固定配置文件还是程序内置常量提供，并明确每张表的元素数量、数据格式和单位。
  2. **频率编码转换关系**：明确CFG的`frequency_code`取值范围，以及显控/湿端下发的实际PWM频率如何转换为该编码；确认该字段是否需要每帧写入。
  3. **W3/W4寄存器协议**：明确W3/W4中哪些字段由PS写入、哪些字段由PL只读，确认每个字段的偏移、数据格式、写使能或提交脉冲时序，以及参数是否在`CBF START`时锁存。

## 四、DMA、Frame和NREAD同步

### 4.1 启动DMA后没有等待DMA完成

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/beam_down.c:1390-1616`
- 已新增`S231_WaitDmaS2mm()`，并在写入`CBF CONTROL.START`之后调用。
- 等待函数轮询`S2MM_DMASR`和每个BD的`status`，检查DMA错误、BD错误、BD完成位和累计传输字节数，并设置1秒超时。
- 当前20ms延时仍只用于DMA预取BD，不再作为DMA完成判断。
- 本项只完成“DMA/BD完成等待”，尚未代表CBF空闲、NREAD中断和显控发送全部完成。

### 4.2 DMA状态清除和读取顺序不正确

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c:1464-1472`
- 现在先读取并保存`S2MM_DMASR`原始值，再判断错误，最后写`S231_DMA_S2MM_IRQ_W1C`清除状态。
- 即使发现启动前错误，也会先保留日志现场，再执行W1C清除，然后放弃当前帧启动，不影响后续帧重新进入初始化流程。

### 4.3 DMA错误掩码不完整

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/beam_down.c:63`
- 当前`S231_DMA_DMASR_ERROR_MASK`为`0x00004070`，没有覆盖文档要求检查的全部S2MM和SG错误位。
- 至少需要核对DMAIntErr、DMASlvErr、DMADecErr、SGIntErr、SGSlvErr、SGDecErr对应位。

### 4.4 BD完成状态和错误状态没有检查

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/beam_down.c:1390-1616`
- `S231_WaitDmaS2mm()`现在逐个检查BD的`status`：bit31完成位、bit30..28错误位以及低位实际传输长度。
- 只有全部BD完成且累计传输长度达到本帧`dma_bytes`要求后，才允许切换IQA/IQB乒乓状态。
- DMA超时或BD错误时，本帧直接返回，不把未完成缓冲区标记为可复用。

### 4.5 单Frame、单BD区、单NREAD缓冲区没有所有权控制

- 状态：`[ ]`
- 优先级：`P0`
- 文件：`src/beam_down.c:1353-1439`、`src/drytoupper.c:300-398`
- IQ有IQA/IQB轮换，但Frame、BD和NREAD都是单缓冲区。
- 当前湿端线程可以在上一帧NREAD发送完成前启动下一帧，并清空NREAD、重写Frame和BD。
- 发送线程此时可能正在从NREAD发送，形成“发送过程中被下一帧覆盖”的数据损坏。
- 必须增加至少一帧在途状态：`FREE -> SUBMITTED -> DMA_DONE -> CBF_DONE -> NREAD_DONE -> SENT -> FREE`，或严格限制下一帧启动。

### 4.7 CBF错误/链路错误的清除和重试策略不完整

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/beam_down.c`、`src/drytoupper.c`
- 已确认业务策略：检测到CBF、链路、NREAD或旧协议包头错误时，只丢弃当前帧/当前结果，不能停止后续湿端数据处理。
- 当前错误路径保持返回或`continue`，不新增断开显控、停止采集或全局复位动作，后续数据仍可继续进入处理流程。

## 五、内存、传感器解析和数据安全

### 5.1 `my_malloc()`按字节增加但实际指针按int移动

- 状态：`[ ]`
- 优先级：`P0`
- 文件：`src/beam_down.c:124-140`
- `memory`是`int[10000]`，但`size`按字节传入并直接执行`memory + top`。
- `top`初始为`-1`，第一次分配返回`memory - 1`附近地址；后续分配还会按4字节放大偏移。
- 这会导致传感器解析结构体错位、覆盖内存和随机崩溃。
- `my_free()`为空实现，当前分配器不能安全回收。

### 5.2 `tfree(NULL)`会直接解引用空指针

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/beam_down.c:tfree()`、`Get_Data_for_Mems()`、`Get_Zpzda_Data_for_Ext_Sensor()`
- `tfree()`现在先检查空指针，避免异常路径下对`NULL`做指针运算和解引用。
- `Get_Data_for_Mems()`单独保存临时缓冲区首地址，解析指针移动后仍使用首地址释放；释放完成后才清空工作指针。
- `Get_Zpzda_Data_for_Ext_Sensor()`在清空指针前完成释放，避免再次传入`NULL`。

### 5.3 NMEA时间临时数组没有保证字符串结束符

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/beam_down.c:613-627`、`src/beam_down.c:569-571`
- `char motion_time_hour[2]`、`motion_time_min[2]`、`motion_time_sec[6]`使用`strncpy()`复制刚好2/6字节，没有留下`\\0`。
- 后续传给`atoi()`和`strtof()`会读取数组边界之外的数据。
- `Ins_zda_time_*`和`Ext_zda_time_*`也需要保证结束符和容量。

### 5.4 传感器数量和偏移读取缺少包内边界检查

- 状态：`[ ]`
- 优先级：`P0`
- 文件：`src/beam_down.c:1999-2019`
- `Copy_IQ_Extsensordata()`先根据IQ包内的数量字段计算多个偏移，然后直接`memcpy()`。
- 当前数量上限判断不能证明“数量字段、传感器区、尾部”全部落在当前湿端包内。
- 异常湿端数据可能使读取位置越过`IQData`实际接收长度。

### 5.5 `my_copy()`会把任意长度向上扩展到64字节

- 状态：`[x]`
- 优先级：`P0`
- 文件：`src/main.c:my_copy()`
- 现在只对完整的64字节块使用NEON汇编复制，剩余尾部按实际长度逐字节复制，不再向上取整。
- 增加了空指针和非正长度判断，长度为0时不会访问任何缓冲区。
- 因此完整湿端包、传感器片段等非64字节对齐长度不会再被额外复制到缓冲区边界之外。

## 六、线程和网络并发

### 6.1 pthread线程入口函数签名不符合`pthread_create()`要求

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/main.c:thread_upper_to_dry()`等线程入口、`include/main.h`
- 所有实际创建的线程入口都已改为`void *thread_xxx(void *arg)`，并显式忽略当前未使用的`arg`。
- 线程函数正常返回`NULL`，与`pthread_create()`要求的函数指针类型一致。
- 头文件中的线程声明也改为带`void *arg`的完整原型，避免旧式无参数声明掩盖类型错误。

### 6.2 `main.c`重新定义系统`pthread_t`

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/main.c:线程数组定义`
- 已删除本地的`typedef unsigned long int pthread_t`，线程数组直接使用系统`pthread.h`提供的`pthread_t`类型。

### 6.3 IQ接收、IQ上传和波束处理之间缺少完整互斥

- 状态：`[ ]`
- 优先级：`P0`
- 文件：`src/wettodry.c:261-279`、`src/wettodry.c:400-412`、`src/drytoupper.c:74-93`、`src/beam_down.c:2028-2093`
- `wet_to_dry()`写入`IQData`时没有和`Send_IQdataToUpper()`、波束处理建立统一的缓冲区所有权。
- `up_iq`开启时，IQ上传线程可能和当前IQ接收/传感器解析同时访问`IQData`。
- 必须明确每个缓冲区的写入者、读取者和释放时机，不能只依赖现有的`mut`局部加锁。

### 6.4 显控断开后的SIGPIPE保护只覆盖新NREAD路径

- 状态：`[x]`
- 优先级：`P1`
- 文件：`src/main.c:main()`
- 程序启动阶段已统一调用`signal(SIGPIPE, SIG_IGN)`，覆盖IQ、状态、配置、传感器和NREAD等全部发送路径。
- 显控断开后，`send()`不再因SIGPIPE直接终止进程，而是返回`EPIPE`或`ECONNRESET`，交由现有连接错误处理流程处理。

### 6.5 `error_process()`跨线程关闭连接且不重置fd

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/tcptrans_link.c:95-116`
- 多个线程都可能调用`error_process()`，同时修改网络状态并关闭`Connect_fd_upper`。
- 关闭后没有立即设置`Connect_fd_upper = -1`，其他线程可能继续对旧fd发送或再次关闭。
- 需要统一连接状态锁和fd生命周期管理。

### 6.6 TCP发送没有统一处理短写

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/drytoupper.c:74-93`及多个`uppertodry.c`发送点。
- 大包调用一次`send()`不保证全部发送完成。
- 只有新的NREAD路径使用了`send_all()`，旧IQ和控制响应仍可能只发出部分数据。

## 七、需要与PL/DSP确认的协议项

### 7.1 `ROLL_INTERVAL = iq_points + 1`是否为固定协议

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/beam_down.c:1180-1183`
- 当前把横摇更新间隔直接设置为`IQ点数+1`。
- 如果协议要求的是固定值、显控配置值或传感器采样间隔，这里会导致PL使用错误的横摇索引。

### 7.2 Frame参数区后13个字段是否仍允许写旧业务值

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/beam_down.c:993-1011`
- 当前向`Ad_Sn_After`、`Recv_SensorSumNum`、`up_iq`、`math_mode`等字段写入旧业务变量。
- 如果新协议将这些字节定义为保留字段，建议置零；如果DSP已经重新定义了含义，需要以最新协议为准。

### 7.3 `SR_LINK_STATUS` bit26的错误含义需要和PL确认

- 状态：`[ ]`
- 优先级：`P1`
- 文件：`src/drytoupper.c:372-377`
- 当前把`SR_LINK_STATUS` bit26当作链路错误位。
- 需要与PL寄存器说明确认该位是错误、未就绪、超时还是其他状态，避免把正常状态误判为失败。

## 当前结论

湿端寄存器映射问题已经完成第一步修正，但当前代码仍不建议直接进行长时间连续采集。至少应优先处理以下P0项：

1. TCP接收长度和IQ包边界校验；
2. IQ/Frame/传感器的一帧对应关系；
3. Frame/NREAD单缓冲区的所有权和覆盖问题；
4. `my_malloc()`和`tfree(NULL)`造成的内存崩溃；
5. `my_copy()`越界复制；
6. IQ接收、IQ上传、波束处理之间的并发访问。

每完成一项，将对应条目的`[ ]`改为`[x]`，并补充实际修改文件、测试方法和测试结果。
