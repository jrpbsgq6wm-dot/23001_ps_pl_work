# PL 配置入口

PL 侧 Codex 只维护本目录及其对应的接口说明，不修改 PS 源码和设备树。

建议提交以下内容：

- PL 配置源文件、约束文件和构建脚本；
- AXI 地址映射、寄存器表、DMA 配置和中断配置；
- PL build ID、生成时间和对应提交号；
- 必要的 `xsa`、`bit` 等发布文件。大文件使用 Git LFS，不提交综合缓存和临时目录。

当前 PL 工程尚未纳入这个 PS 仓库。把 PL 工程放入本目录前，先在共享/协议/ 和 docs/协作记录/ 中确认目录、接口和版本对应关系。
