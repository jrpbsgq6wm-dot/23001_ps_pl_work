# PS 工程

本目录统一保存 PS 侧内容：

- `20260916/`：PS 源码及编译工程文件；
- `device-tree/`：设备树源文件；
- 其他 PS 配置、启动文件和部署说明。

PS 侧 Codex 可以修改本目录。PL 侧 Codex 只读取本目录，不直接修改其中的源码和设备树。

PS 侧使用的 PL 寄存器、数据帧、DMA、中断和 DSP 接口，以共享/协议/ 中双方确认的内容为准。
