# 23001 PS/PL 协作仓库

目录 `20260916/` 是 PS 工程快照，`device-tree/` 是设备树，`pl/` 是 PL 配置入口，`docs/` 保存接口结论、问题和联调记录。

当前仓库由原有 `20260916` 源码目录复制建立，原源码目录仍然保留，不作为两台电脑之间的共享工作目录。正式基线提交前，请检查 `20260916/` 中的源码是否包含当前需要保留的未提交改动。

GitHub 远程仓库建立后，两台电脑分别克隆本目录，不要让两个 Codex 会话使用同一个工作目录。

